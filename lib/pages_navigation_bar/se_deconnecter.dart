import 'package:flutter/material.dart';

class SeDeconnecter extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Testing & Debugging'),
          centerTitle: true,
          backgroundColor: Colors.red,
        ),
      );
}
