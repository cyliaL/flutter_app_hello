import 'package:flutter/material.dart';

class MesAchats extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Deployment'),
          centerTitle: true,
          backgroundColor: Colors.teal,
        ),
      );
}
